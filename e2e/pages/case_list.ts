import hasSelector from '../helpers/browser_helper';

const I = actor();
const selectors = {
  hideFilterButton: 'Hide Filter',
  shareCaseButton: 'Share Case',
  jurisdictionDropdown: '#wb-jurisdiction',
  caseTypeDropdown: '#wb-case-type',
  stateDropdown: '#wb-case-state',
  applyButton: 'Apply',
  resetButton: 'Reset',
};

class CaseListPage {
  
};

export default new CaseListPage();