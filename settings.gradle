pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
        mavenCentral()
        maven {
            url "https://jitpack.io"
        }
    }
}

rootProject.name = 'sptribs-case-api'

// Uncomment if you wish to debug configGenerator locally
/*var configGenerator = '../dtsse-ccd-config-generator'
if (file(configGenerator).exists()) {
  includeBuild configGenerator
}*/
